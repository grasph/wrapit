module_name         = "Hello"

input               = [ "A.h" ] 

include_dirs        = [ "@CMAKE_SOURCE_DIR@" ]

cxx-std             = "c++17"

# all generated code in a single file:
n_classes_per_file = 0
